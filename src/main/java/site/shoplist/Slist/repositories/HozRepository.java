package site.shoplist.Slist.repositories;

import org.springframework.data.jpa.repository.JpaRepository;
import site.shoplist.Slist.models.Hoz;

public interface HozRepository extends JpaRepository<Hoz, Integer> {
}
